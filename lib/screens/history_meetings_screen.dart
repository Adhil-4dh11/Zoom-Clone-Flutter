import 'package:flutter/cupertino.dart';

class HistoryMeetingsScreen extends StatelessWidget {
  const HistoryMeetingsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
